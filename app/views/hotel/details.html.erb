<h1><%= "Details Of Room #{@room.number}"%></h1>

Number: <%= @room.number %><br/>
Extension: <%= @room.extension %><br/>
Room type: <%= @room.room_type.name %><br/>
Description: <%= @room.description %><br/>
    
<h2> List of Occupants </h2> 
<table>
  <tr>
  	<th>No.</th>
    <th>Occupant Name</th>
    <th>Personal ID Number</th>
    <th></th>
    <th></th>
  </tr>
  
  <% @room.current_occupancies.each_with_index do |occ, index| %>
  	<tr>
    	<td><%= index + 1 %></td>
    	<td><%= occ.guest.name  %></td>
    	<td><%= occ.guest.personal_id_number %></td>
    	<th><%= link_to 'Move', move_path(:occu_id => occ.id) %></th>
	</tr>
  <% end %>
</table>
<%= link_to 'Back', occupancies_path %>
